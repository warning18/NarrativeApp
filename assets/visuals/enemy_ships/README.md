Drop image files for the "enemy_ships" collection here (e.g. `sword_iron.png`).

Reference the filename (not the full path) in that record's "Visual Asset"
field in the Data tab — the app resolves it to `assets/visuals/enemy_ships/<filename>`.
